<Tabs tabs="{{tabs}}" bindtabsltemChange="handleTabsltemChange">
  <block wx:if="{{tabs[0].isActive}}">
    <view class="order_main" wx:for="{{15}}">
      <view class="order_item">
        <view class="order_no_row">
          <view class="order_no_text">订单编号</view>
          <view class="order_no_value">HD22045227805636662</view>
        </view>
        <view class="order_no_row">
          <view class="order_price_text">订单价格</view>
          <view class="order_price_value">$8999</view>
        </view>
        <view class="order_no_row">
          <view class="order_no_text">订单日期</view>
          <view class="order_no_value">2020/12/1 上午10：06：58</view>
        </view>
      </view>
    </view>
  </block>
  <block wx:elif="{{tabs[1].isActive}}">1</block>
  <block wx:elif="{{tabs[2].isActive}}">2</block>
  <block wx:elif="{{tabs[3].isActive}}">3</block>
</Tabs>